<UserControl x:Class="Rain.Designer.Views.Mesh.NodeDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rain.Designer.Views.Mesh"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<StackPanel>
		<Button Command="{Binding DataContext.RemoveNodeCommand, RelativeSource={RelativeSource AncestorType=local:MeshControl}}"
				CommandParameter="{Binding Position}">
			<Button.Template>
				<ControlTemplate>
					<Border BorderBrush="Gray"
							BorderThickness="1">
						<TextBlock Text="Remove"
								   TextAlignment="Center"
								   Padding="10 5"/>
					</Border>
				</ControlTemplate>
			</Button.Template>
		</Button>
		<TextBlock Text="{Binding Position.X}"/>
		<TextBlock Text="{Binding Position.Y}"/>
	</StackPanel>
</UserControl>
