<UserControl
	x:Class="Rain.Designer.Views.Tree.TreeDesignerControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:Rain.Designer.Views.Tree"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:waves="clr-namespace:Rain.Designer.Views.Waves"
	d:DesignHeight="450"
	d:DesignWidth="800"
	mc:Ignorable="d">
	<Grid Width="600">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="5" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid Grid.Column="0" Background="WhiteSmoke">
			<local:ConnectionsControl DataContext="{Binding Nodes}" />
			<local:NodesControl DataContext="{Binding Nodes}" />
		</Grid>
		<GridSplitter
			Grid.Column="1"
			Width="5"
			HorizontalAlignment="Stretch"
			Background="DarkGray" />
		<UniformGrid Grid.Column="2" Columns="2">
			<local:NodeEditorControl DataContext="{Binding SelectedNode}" />
			<waves:WaveBlockControl DataContext="{Binding SelectedNode.WaveBlock}" />
		</UniformGrid>
	</Grid>
</UserControl>
